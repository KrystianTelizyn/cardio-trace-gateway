[request_definition]
r = role, method, path

[policy_definition]
p = role, method, path

[policy_effect]
e = some(where (p.eft == allow))

[matchers]
m = r.role == p.role && r.method == p.method && keyMatch2(r.path, p.path)
